# Commands

- Start Expo development server: `npx expo start`
- Run Android app (development build): `npx expo run:android`
- Run iOS app (development build): `npx expo run:ios`
- Lint: `npm run lint`
- Type check: `npm run typecheck`
